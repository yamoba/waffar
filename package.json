{
  "name": "waffar-platform",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix backend ci && npm --prefix backend run prisma:generate && npm --prefix backend run build && npm --prefix frontend ci && npm --prefix frontend run build",
    "start": "node scripts/start-production.mjs",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "test": "npm --prefix backend test -- --run"
  }
}
