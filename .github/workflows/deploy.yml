name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Build and push Docker images
        run: |
          echo "Build step — replace with actual registry push"
          docker compose build

      - name: Deploy to server
        run: |
          echo "Deploy step — replace with actual deployment (SSH, k8s apply, etc.)"
          echo "Example: ssh deploy@server 'cd /opt/waffar && git pull && docker compose up -d --build'"
